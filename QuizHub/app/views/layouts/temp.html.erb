<!DOCTYPE html>
<html>
<head>
	<title>.:The Quiz Hub:.</title>
<%= stylesheet_link_tag('application', :media => 'all')%>
  	<title>.:The Quiz Hub:.</title>
</head>
<body>
	<header>
		<div id ="title">The QuizHub</div>
	</header>
<div id = "hd1"></div>
  <% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash#{name}" %>
  <% end %>

<%= yield %>
<div id= "footer">&copy; Damilola and Dewunmi</div>
</body>
</html>